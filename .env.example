# Spotify App Credentials
# Get these from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here

# Redirect URI (must match what's configured in Spotify Dashboard)
# For local dev: http://127.0.0.1:5173/api/auth/callback
# For production: https://your-app.vercel.app/api/auth/callback
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/api/auth/callback

# Upstash Redis (for song caching - required for production)
# Get these from https://console.upstash.com/ after creating a Redis database
# You can also add Upstash as a Vercel integration for automatic env var setup
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token_here
